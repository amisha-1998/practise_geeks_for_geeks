int transitionPoint(int arr[], int n)
{
    // code here
    /*int l=0,r=n-1,mid;
    if(arr[n-1]==0)
        return -1;
    else
    {
        mid = low+(high-low)/2;
        if(arr[mid]==0)
        
    }*/
    int i,count=0;
    for(i=0;i<n;i++)
    {
        if(arr[i]==1)
            break;
        else
            count++;
    }
    if(count==n)
        return -1;
    else
        return count;
}
